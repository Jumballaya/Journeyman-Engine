file(GLOB_RECURSE MEMORY_SOURCES CONFIGURE_DEPENDS
    *.cpp
)

file(GLOB_RECURSE MEMORY_HEADERS CONFIGURE_DEPENDS
    *.hpp
)

add_library(engine_memory STATIC ${MEMORY_SOURCES} ${MEMORY_HEADERS})
target_include_directories(engine_memory PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})